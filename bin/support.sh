#!/bin/sh

install_cache_dirs() {
    # npm_cache_dir=$(npm config get cache)
    # APP_CACHE=$BUILD_DIR/.heroku/cache

    # mkdir -p $APP_CACHE

    # mkdir -p $APP_CACHE/node_modules
    # mkdir -p $APP_CACHE/bower_components
    # mkdir -p $APP_CACHE/npm_cache
    # mkdir -p $APP_CACHE/bundle
    # mkdir -p vendor/

    # rm -rf node_modules
    # rm -rf assets/bower_components
    # rm -rf $npm_cache_dir
    # rm -rf vendor/bundle

    # ln -sf $APP_CACHE/node_modules node_modules
    # ln -sf $APP_CACHE/bower_components assets/bower_components
    # ln -sf $APP_CACHE/npm_cache $npm_cache_dir
    # ln -sf $APP_CACHE/bundle vendor/bundle


}
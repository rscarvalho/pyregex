language: python
python:
  - "3.7.7"

node_js:
  - 0.10.13

ruby:
  - 1.9.3-p429

install:
  - pip install -qr pipenv
  - pipenv install
  - ./install_assets_deps.sh
  - grunt clean build

script:
  - pipenv run tests
  - grunt test
